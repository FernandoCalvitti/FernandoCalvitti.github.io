import React from "react";
import SmallTitle from "../SmallTitle";

const ExpButton = ({ name }) => {
  return <SmallTitle text={name} style={{ textTransform: "uppercase" }} />;
};

export default ExpButton;
